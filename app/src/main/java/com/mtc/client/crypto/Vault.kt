package com.mtc.client.crypto

/**
 * Local Vault placeholder.
 * Full Argon2id + AES-256-GCM implementation will be re-added with matrix-rust-sdk.
 * For now this is a stub so the demo UI builds cleanly.
 */
class Vault {
    fun isReady(): Boolean = true
}
